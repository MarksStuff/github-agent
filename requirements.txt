# When adding pip's to this file, do it in a separate PR that can be merged before the code that needs it.
# Because we need to rebuild the docker image for our CI builds with this pip to make the new code pass.
aiohttp>=3.8.0
anthropic
bandit
black
cachetools>=5.0.0
coverage
fastapi>=0.104.0
GitPython
httpx
mypy
pip
pydantic>=2.0.0
PyGithub
pyright
python-lsp-jsonrpc
python-lsp-server[all]>=1.8.0
pytest
pytest-asyncio>=0.23.0
pytest-cov
pytest-env
python-dotenv
psutil>=5.8.0
requests
ruff==0.1.13
types-requests
uvicorn[standard]>=0.24.0
watchdog>=3.0.0

# LangGraph and LangChain dependencies for multi-agent workflow
langgraph>=0.0.20
langgraph-checkpoint-sqlite>=1.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-ollama>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.1.0

# Async support
asyncio
aiosqlite

# Async database for checkpointing
aiosqlite

# Additional testing utilities for workflow
factory-boy>=3.2.0
freezegun>=1.2.0
pytest-mock>=3.10.0

# LangGraph Studio and API server
langgraph-cli>=0.1.50

